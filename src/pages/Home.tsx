import React from 'react'

export default function Home() {
  return (
    <section className="panel">
      <header className="panel-header">
        <h1>Welcome to MyApp</h1>
      </header>
      <p style={{ marginTop: 12 }}>
        This demo implements routing and nested routing using React Router DOM. Use the
        navigation bar to explore pages and nested sections.
      </p>
    </section>
  )
}
